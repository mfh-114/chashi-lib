package org.mfh114.chashi.scanner;

/**
 * ScannerEngine is Finite State Machine (FSM). It processes few determined
 * states. It scans the Matrix and create workers threads to perform the
 * topological sorting. Add sorted items in the list.
 */
public class ScannerEngine {

	
	
}
